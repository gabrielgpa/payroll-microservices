spring:
  application:
    name: hr-oauth
  cloud:
    config:
      uri: http://hr-config-server:8888
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: "*"