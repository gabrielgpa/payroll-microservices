spring:
  application:
    name: hr-api-gateway-zuul
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: "*"