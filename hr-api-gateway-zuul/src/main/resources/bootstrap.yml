spring:
  application:
    name: hr-api-gateway-zuul
  cloud:
    config:
      uri: http://hr-config-server:8888
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: "*"